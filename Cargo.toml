[package]
name = "alpenglowed"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
gpui = "0.2"
fuzzy-matcher = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
